<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.aegis</groupId>
  <artifactId>aegis-parent</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>
  <name>AEGIS</name>
  <description>AEGIS - Semantic Web Attack Detection Platform</description>

  <modules>
    <module>aegis-core</module>
    <module>aegis-agent</module>
    <module>aegis-console</module>
    <module>aegis-gateway</module>
    <module>vuln-target</module>
  </modules>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>
    <!-- 保留方法参数名到字节码。Spring Data JPA 的 @Param 与
         Spring MVC 的 @RequestParam 均依赖反射读取参数名，
         缺失将导致运行时抛出 "parameter name information not available"。
         此处使用 property 形式声明，可靠地作用于所有子模块，
         避免被 Spring Boot parent 的插件配置覆盖。 -->
    <maven.compiler.parameters>true</maven.compiler.parameters>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring.boot.version>3.2.5</spring.boot.version>
    <jsqlparser.version>4.7</jsqlparser.version>
    <jsoup.version>1.17.2</jsoup.version>
    <bytebuddy.version>1.14.12</bytebuddy.version>
    <caffeine.version>3.1.8</caffeine.version>
    <jjwt.version>0.12.5</jjwt.version>
    <junit.version>5.10.2</junit.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.aegis</groupId>
        <artifactId>aegis-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.jsqlparser</groupId>
        <artifactId>jsqlparser</artifactId>
        <version>${jsqlparser.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jsoup</groupId>
        <artifactId>jsoup</artifactId>
        <version>${jsoup.version}</version>
      </dependency>
      <dependency>
        <groupId>net.bytebuddy</groupId>
        <artifactId>byte-buddy</artifactId>
        <version>${bytebuddy.version}</version>
      </dependency>
      <dependency>
        <groupId>net.bytebuddy</groupId>
        <artifactId>byte-buddy-agent</artifactId>
        <version>${bytebuddy.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.ben-manes.caffeine</groupId>
        <artifactId>caffeine</artifactId>
        <version>${caffeine.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <source>17</source>
            <target>17</target>
            <encoding>UTF-8</encoding>
            <compilerArgs>
              <!-- 保留方法参数名到字节码中。
                   Spring MVC 的 @RequestParam / @PathVariable 与
                   Spring Data JPA 的 @Param 均依赖反射获取参数名，
                   缺少此标志会导致运行时抛出
                   "parameter name information not available" 异常。 -->
              <arg>-parameters</arg>
            </compilerArgs>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.2.5</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
